// Transitional re-export shim. The evaluator now lives in the pure
// `@major-vis/degree-audit` package; apps import it there (see the import map).
export * from '../packages/degree-audit/planner.js'
