// Transitional re-export shim. Schedule generation now lives in the pure
// `@major-vis/schedule-core` package; apps import it there (see the import map).
export * from '../packages/schedule-core/generate.js'
